import Link from "next/link";

export default function SavedReport(params: any) {
    
    return (
        <div>
            <Link href="/">Back to home</Link>

            saved report
        </div>
    );
}